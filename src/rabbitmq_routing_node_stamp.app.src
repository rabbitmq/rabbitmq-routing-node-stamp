{application, rabbitmq_routing_node_stamp,
 [{description, "Stamps a message with the name of the RabbitMQ node that accepted it from the client."},
  {vsn, ""},
  {modules, []},
  {registered, []},
  {applications, [kernel, stdlib, rabbit, amqp_client]}]}.
